var React = require('react');

var AddScheduleModal = React.createClass({
  render: function() {
      return (
        <div>
          <p>An Add Schedule Modal</p>
        </div>
      );
  }
})

module.exports = AddScheduleModal
