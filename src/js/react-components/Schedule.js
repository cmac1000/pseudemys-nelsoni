var React = require('react');

var Schedule = React.createClass({
  render: function() {
      return (
        <div>
          <p>A Schedule</p>
        </div>
      );
  }
})

module.exports = Schedule
