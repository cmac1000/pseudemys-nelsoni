var q = require('q');
var imageURLS = require('../../data/image_urls')

// Wikimedia Commons, I ask your forgiveness.
var NelsoniAPI = {
  getImageURL: function(image_name) {
    return imageURLS.imagename
  }
}

module.exports = NelsoniAPI
